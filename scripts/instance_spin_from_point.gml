///instance_spin_from_point( pointX, pointY, radius, angle );

var xx = argument[0];
var yy = argument[1];
var radius = argument[2];
var angle = argument[3];


x = xx + lengthdir_x( radius, angle );
y = yy + lengthdir_y( radius, angle );
